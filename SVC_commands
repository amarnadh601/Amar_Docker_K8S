$ kubectl create -f svc.yml
$kubectl apply -f svc.yml
$ kubectl get svc
$ kubectl describe svc hello-svc

The following commands list  Endpoint 

$kubectl get ep
$kubectl describe ep hello-svc

#Deployment commands
kubectl rollout history deployments companyms-deploy
kubectl rollout undo deployment companyms-deploy --to-revision=2